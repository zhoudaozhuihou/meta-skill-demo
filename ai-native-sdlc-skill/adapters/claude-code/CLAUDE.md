# Repository Instructions

## Engineering workflow

For non-trivial work, use the `ai-native-sdlc` skill and maintain change-scoped artifacts under `docs/changes/<CHANGE-ID>/`.

## Commands

Replace with repository-owned commands:

```text
build: <command>
test: <command>
lint: <command>
verify: <command>
```

## Architecture

Document only stable repository architecture here.

## Protected / Generated Paths

Document paths agents should not manually edit.

## Common Agent Mistakes

Add concise repository-specific rules only after repeated evidence of the same mistake.
