# GitHub Copilot Adapter

Use `.github/copilot-instructions.md` for stable repository-level expectations. Keep detailed lifecycle behavior in the reusable `ai-native-sdlc` skill and reference change-scoped artifacts from issues/PRs or agent tasks.

For enforceable controls, use branch protection, required checks, CODEOWNERS, Actions, secret scanning, and environment approvals rather than instructions alone.
