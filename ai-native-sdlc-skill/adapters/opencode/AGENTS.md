# OpenCode Engineering Guide

Use `skills/ai-native-sdlc/SKILL.md` for lifecycle orchestration.
Persist significant work in `docs/changes/<CHANGE-ID>/`.
Run repository-owned verification before completion.
Treat production deployment and destructive actions as privileged operations requiring policy-defined approval.
