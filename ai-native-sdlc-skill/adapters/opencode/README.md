# OpenCode Adapter

Map your OpenCode agent configuration to:

- repository instructions (`AGENTS.md` or equivalent);
- the `ai-native-sdlc` orchestrator;
- narrow role prompts from `skills/ai-native-sdlc/agents/`;
- deterministic shell/CI controls;
- change-scoped artifact storage.

Keep runtime configuration separate from the skill so the lifecycle remains portable.
