# Source Precedence

Default conflict precedence:

1. approved design standard;
2. token package;
3. Tailwind/theme config;
4. shared UI library;
5. repeated production usage;
6. legacy usage;
7. agent inference.

Record meaningful conflicts in `evidence.json`.
