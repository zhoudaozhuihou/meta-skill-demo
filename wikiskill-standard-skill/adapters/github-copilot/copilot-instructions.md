# WikiSkill Integration

For experience-driven skill improvement tasks, follow the repository's WikiSkill package.

Treat:
- `raw/` as immutable evidence,
- `wiki/` as persistent accumulated knowledge,
- `skills/` as validation-gated executable procedures.

Generate candidate skill changes as reviewable artifacts; do not silently overwrite accepted skills.
