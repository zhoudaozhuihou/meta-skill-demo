# WikiSkill Integration

Use `wikiskill/SKILL.md` for tasks involving automatic skill evolution.

The default optimization loop is:
rollout -> consolidate wiki -> propose one atomic skill change -> validate -> accept or rollback -> record impact.
